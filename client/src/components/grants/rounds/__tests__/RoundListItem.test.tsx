// import React from "react";
// import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom";

describe("Display Round List Items", () => {
  it("should show active rounds", () => {
    expect(true).toBe(true);
  });
  it("should show current round applications", () => {
    expect(true).toBe(true);
  });
  it("should show past rounds", () => {
    expect(true).toBe(true);
  });
});
